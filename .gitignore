# Compiled binary
/photo-gallery

# Persisted index cache
/gallery-cache.gob
/gallery-cache.gob.tmp

# Local environment config
/.env

# Local photo library (keep the directory, ignore its contents)
/photos/*
!/photos/.gitkeep
